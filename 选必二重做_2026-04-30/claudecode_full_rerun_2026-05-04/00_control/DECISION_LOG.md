# 决策日志（DECISION_LOG）

> 独立生产线 B 所有可审计决策。每条都要写为什么。

## 2026-05-04 D1 — extracted_text 池可作"独立读源通道"
- 用户要求"独立扫描源文件"；但 bash sandbox 不挂 `/Users/wanglifei/Desktop/2024模拟题` 等路径。
- 决策：把 `选必二重做_2026-04-30/extracted_text/` 视为原始文本抽取（等同于本线自跑 pdftotext/docx2txt 的输出），可作为本线读源通道；但凡判断、归位、踩分句必须由本线在原文文本基础上独立得出，不得读 Codex 已经写好的 CSV/MD/JSON 中的"结论"。
- 风险：抽取质量可能有 OCR/切分误差。
- 缓解：抽取文本与原始 PDF/DOCX 路径全部进入 `SOURCE_LEDGER.csv` 的 `source_path` 与 `extracted_path` 双字段；如发现关键题面/细则在抽取文本中明显残缺，标 `extraction_uncertain`，不推动主干。

## 2026-05-04 D2 — 真实 GPT/Claude 调用本会话不能完成 → real_call_pending
- 本会话无外网/外部模型调用通道；用户硬规则禁止冒名"已审"。
- 决策：在 `gpt55_review/` 与 `opus47_review/` 输出独立可用的审稿包；提交人工驱动；最终验收报告里相关 gate 全部标 `real_call_pending`，不写 PASS。
- 用户可在外部网页端 GPT-5.5 Pro / Claude Opus 4.7 Adaptive 模式下复制 prompt 跑出来后回贴。

## 2026-05-04 D3 — `2026石景山期末` 排除
- 用户硬规则；本线不处理。

## 2026-05-04 D4 — 期中卷的处理基线
- 期中卷可能不出选必二；本线先标 `uncertain`，逐题面扫到法律情景才升为 `has_xuanbier`；若全卷无选必二法律情景 → 标 `no_xuanbier` 并跳过题包构建。

## 2026-05-04 D5 — 选择题与主观题分离
- 用户硬规则；本线对每套卷分别建立 `subjective_questions` 与 `choice_questions` 两个独立分支；归位时各自归位，不交叉。

## 2026-05-04 D6 — 五域划分依据（草案，待证据收敛后再回写）
- 交易交换域：合同 + 消费者 + 格式条款 + 劳务承诺与履行 → 共同处理"交易关系中的承诺—履行—信息不对称—权益保护"。
- 身份财产域：物权 + 相邻 + 婚家继 → 共同处理"基于身份/物的归属与排他/分配"。
- 人身权益域：人格权 + 一般侵权 + 特殊侵权 → 共同处理"对自然人人身/人格的损害与救济"。
- 创新竞争域：知识产权 + 不正当竞争 → 共同处理"以智力成果/竞争行为为客体的法益与边界"。
- 救济公共域：调解 + 仲裁 + 诉讼 + 举证 + 执行 + 公益 + 生态 → 共同处理"争议进入程序后的处理路径与公共法益保护"。
- 待考过题归位完成后再写正式划分依据，并放入框架版 Word 卷首。
