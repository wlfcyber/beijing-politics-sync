# ClaudeCode Batch 002 Field Repair Prompt

修复文件：

`reports\选必一_哲学宝典式重建_2026-05-16\02_claudecode_batches\BATCH_002_CLAUDECODE_DRAFT.md`

只做结构修复，不改术语内容、来源判断、材料触发、答案句实质。

当前问题：文件有 20 个 `**术语：` 条目，但 `完整设问` 和 `细则位置` 字段各只有 18 个。原因是少数合并条目使用了类似 `完整设问与细则位置（合并采分核心，保留来源差异）：` 的合并字段。

请把每个 `**术语：` 条目都修成严格字段格式：

```markdown
- 完整设问：...
- 细则位置：...
- 来源：...
- 材料触发：...
- 答案句：...
```

对跨题合并条目，可以在 `完整设问：` 里列出多个设问，在 `细则位置：` 里列出多个细则位置，但字段标签必须分开。

修复后请确认：

- `**术语：`、`完整设问`、`细则位置`、`来源`、`材料触发`、`答案句` 六项数量一致。
- 答案句仍不含：`采分点`、`要落到`、`设问要求`、`细则要求`、`本题需要`、`证据层级`、`v7`、`材料中`。

只写入文件，不要输出完整文档。
