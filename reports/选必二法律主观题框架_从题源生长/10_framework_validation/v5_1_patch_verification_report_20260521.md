# V5.1 补丁核验报告

生成时间：2026-05-21

## 结论

V5.1 已完成 Claude Opus 对 V5 提出的硬问题修补，可以作为“下一轮干净题面零基础压测”和“35 道核心题扩展”的候选输入。

V5.1 不能作为最终宝典，也不能声明全流程 PASS。

## 已修补

1. 框架结构：由 V5 的七张动作卡修为“六张并列动作卡 + 起手表态铁律”。
2. 节点命名：`切责成链` 改为 `分关系·定责任`，`护创新` 改为 `认产权·抓侵权`，`走救济` 改为 `排维权步骤`，`补价值` 改为 `推价值`。
3. CC0077 样章：补入顾氏三兄弟案，避免漏掉表格后半题。
4. CC0143 样章：补入《消费者权益保护法》全称、合同可撤销、请求撤销合同、欺诈惩罚性赔偿。
5. CC0103 样章：保留“推动法治中国建设”，但要求由本案知识产权保护、司法公信力和同类案件示范推出。
6. CC0277 证据清洗：V5.1 只保留 `R_CC0277_2026_房山_二模_18_01` 至 `_06`，不再引用 `_07` 至 `_11` 的哲学原子。
7. CC0251 证据清洗：V5.1 只保留 `R_CC0251_2026_丰台_一模_20_01`，不再引用 `_02` 至 `_16` 的学生问题、教学建议、复练题和跨模块话术。
8. `RECOVER_2026_西城_二模_18_2` 降为 AI 宏观开放容器，不作为 `划风险边界` 或 `推价值` 核心支撑。
9. 低频 formal singleton 列入开放容器，不作为核心动作卡支撑。

## 机器核验

- V5.1 关键输出均已生成。
- `framework_v5_1_evidence_map_20260521.csv` 当前共 31 条节点-题目关系：`core_support=23`，`source_check_pending=8`。
- `v5_1_batch_plan_20260521.csv` 覆盖 65 道题：`formal=61`，`reference_only=4`。
- 对 V5.1 框架、证据映射、十题样章和宝典样章 grep：
  - `R_CC0277_..._07` 至 `_11`：0 命中。
  - `R_CC0251_..._02` 至 `_16`：0 命中。
- CC0251 当前映射只含 `R_CC0251_2026_丰台_一模_20_01`。

## 未关闭

1. 21 个设问字段仍需回源或标注 inferred，见 `10_framework_validation/v5_ask_text_source_check_queue_20260521.csv`。
2. CC0251、CC0283、CC0277 等仍是 `source_check_pending`，不得进入学生最终核心样章扩展。
3. V5.1 仍只有十题样章，不是全 65 逐题宝典。
4. 还没有用“干净题面、无答案、无细则”的题包重新做零基础学生压测。
5. 尚未把 V5.1 回传 GPTPro 和 Claude Opus 做交叉复核。

## 下一步

1. 制作 V5.1 干净题面压测包，只给学生版一页纸和无答案题面。
2. 让零基础学生模拟器重做 5-8 题，并按细则评分。
3. 若仍不能接近满分，先补“最小法律规则句库”，不急着扩展全书。
4. 压测通过后，再扩展 35 道核心题样章。
