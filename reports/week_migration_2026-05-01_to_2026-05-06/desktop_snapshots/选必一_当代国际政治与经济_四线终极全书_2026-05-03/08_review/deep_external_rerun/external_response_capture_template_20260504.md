# 外部回复保存模板

## GPT-5.5 Pro

time:
conversation:
model/mode_visible:
capture_method:
attachment_used:
prompt_used:

```text
PASTE_GPT_RESPONSE_HERE
```

## Claude Opus 4.7 Adaptive

time:
conversation:
model/mode_visible:
capture_method:
attachment_used:
prompt_used:

```text
PASTE_CLAUDE_RESPONSE_HERE
```

## Capture Notes

- If copy button returns stale text, write that explicitly and use visible browser transcription only with a note.
- If response is partial/truncated, mark `partial_capture` and do not treat as PASS.
- If either external model gives only generic praise, ask one follow-up for concrete issue table before accepting the gate.
