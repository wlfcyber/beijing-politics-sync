# Codex A 补丁者 Batch02 Review

复查对象：

- `codex_lane/agents/worker/worker_batch02_entries.md`
- `codex_lane/agents/worker/worker_batch02_source_notes.csv`
- `02_extraction/codex_extraction_logs/batch02_source_locator_notes.md`
- `fusion/merge_register_batch01.md`
- `feige-politics-garden-xuanbiyi/references/xuanbiyi-term-protocol.md`

结论：`PASS_WITH_FIXES`

Batch02 可以进入下一步融合，但不能无修补进入学生稿或频次统计。主要风险不是漏收，而是：海淀二模 Q21 证据等级必须沿用 worker 的保守标法；顺义 Q20 的经济全球化短表述必须并入朝阳一模的完整高信息核心；顺义同一政治评分位中的替代表述不能重复计数。

## 1. 海淀二模 Q21 证据等级

状态：`PASS`

- worker 将 Q21 标为 `P1 scoring-structured answer with P0 marking-record support；不标为纯 P0 正式细则`，保守合适。
- source_notes 同样把 `细则.docx` 标为 `P1_scoring_structured_answer`，把 `评标实录.docx` 标为 `P0_marking_record_support`，没有把结构化答案直接冒充正式评分细则。
- locator notes 中“P0 + evaluation-record source”表述偏强；后续融合应继承 worker/source_notes 的保守口径，而不是 locator 的短语。
- 这些条目可入主表候选，但教师核查层必须显示：`P1 structured scoring answer + P0 marking-record support`，不得混入纯 P0 频次。

## 2. 朝阳一模 Q20 经济全球化完整表述

状态：`PASS`

- 完整高信息表述已保留：`推动经济全球化朝着更加开放、包容、普惠、平衡、共赢的方向发展`。
- worker 没有把它压成 `经济全球化正确方向`，答案句也保留了 `开放、包容、普惠、平衡、共赢`。
- 同题的 `两个市场、两种资源`、`贸易投资自由化便利化`、`促进人才、商品、服务和生产要素在全球范围内流动` 已分点列出，未粗暴合并。
- `高水平对外开放` 被标为模块边界，不作为本批主术语，符合协议。

## 3. 顺义一模 Q20 多点与合并风险

状态：`PASS_WITH_FIXES`

- `共同利益` 单独列为必答 1 分，且材料触发聚焦“合作为什么成立、能持续”，归理论桶，不能与正确义利观合并。
- `外交思想：构建人类命运共同体` 与替代表述 `独立自主的和平外交政策 / 外交政策宗旨 / 义利观` 被 worker 作为同一评分功能记录，这是可以的；但融合时必须作为同一政治评分位的可用替代表述，不得同时给 M05 正确义利观和 M06 人类命运共同体各加一次独立出现。
- `新型国际关系：合作共赢/互利共赢/共享发展成果` 应归政治多极化的合作共赢/新型国际关系核心，不与理论桶的共同利益合并。
- `经济全球化方向：普惠、平衡、共赢` 是源中低信息短表述。融合时必须并入朝阳一模 Q20 的完整核心 `推动经济全球化朝着更加开放、包容、普惠、平衡、共赢的方向发展`，把 `普惠、平衡、共赢` 作为表述积累，不得另起低信息核心。
- `时代主题：和平和发展` 应并入 M01 `和平与发展仍是时代主题`，不另起顺义独立核心。

## 4. 海淀期末 Q22 P2 边界

状态：`PASS`

- worker 明确写明证据状态为 `P2，教师研修 PPT 中第22题细则与可选知识；不是正式评标/评分细则 P0`。
- source_notes 也标为 `P2_teaching_lecture`，且说明选必一是可选知识，不是唯一必答点。
- 该条只能作为 P2 综合短文可选角度候选，不能冒充普通选必一主观题 P0 评分点，也不能无标记给 `人类命运共同体 / 中国智慧中国方案` 增加 P0 频次。

## 5. 答案句后台话

状态：`PASS_WITH_FIXES`

- 未发现答案句中出现硬禁词：`材料中`、`采分点`、`要落到`、`细则要求`、`设问要求`、`本题需要`、`v7`、`v8`。
- worker 文件中的 `P0/P1/P2/P3`、路径、`证据状态`、`评标实录` 等属于内部核查信息，可以保留在 worker/review 层，但不得进入学生稿。
- 轻微修辞风险：海淀期末 Q22 答案句中的“可以转化为”偏讲评语气，学生稿中建议改成更像卷面句，例如“‘愚公移山’体现团结奋斗、迎难而上的精神，中国以此推动构建人类命运共同体，贡献中国智慧和中国方案。”

## 6. 对照 Batch01 Merge Register 的补丁要求

1. M01：把朝阳一模 `中国的做法符合时代主题和平与发展`、顺义 `时代主题：和平和发展` 并入 `和平与发展仍是时代主题`，作为表述积累。
2. M03：朝阳一模 `推动建设合作共赢的新型国际关系`、顺义 `新型国际关系：合作共赢/互利共赢/共享发展成果` 应归政治多极化/新型国际关系核心，和共同利益保持 non-merge。
3. M05/M06/M07：朝阳一模、顺义、海淀二模、海淀期末中的正确义利观/HMC/中国智慧中国方案要按评分位置和证据等级计数；替代表述不得重复膨胀频次。
4. M12：朝阳一模 `两个市场、两种资源` 可与 Batch01 两个市场两种资源核心合并；保留 P0 来源。
5. 新增经济全球化方向核心：用朝阳一模完整表述作核心名，顺义 `普惠、平衡、共赢` 作变体；不得反向压缩。
6. 海淀二模 Q21 与联合国桶合并时，保留 `P1 structured scoring answer + P0 marking-record support` 标签；不要在 M04 或联合国新核心里变成纯 P0。

## 下一步补丁清单

1. 融合前新增/更新经济全球化方向核心：`推动经济全球化朝着更加开放、包容、普惠、平衡、共赢的方向发展`。
2. 为顺义 Q20 的 `义利观 / HMC / 外交政策宗旨` 写清“同一评分位替代表述”，避免 M05/M06/M13 重复计数。
3. 统一海淀二模 Q21 证据标签为 `P1 structured scoring answer + P0 marking-record support`，覆盖 locator 的偏强 P0 表述。
4. 海淀期末 Q22 保持 P2 综合短文可选角度；不得进入 P0 频次。
5. 学生稿生成前，删除 worker 层证据状态、路径、评标实录、P0/P1/P2/P3 等后台信息，并把 Q22 答案句改成卷面句。

最终判定：`PASS_WITH_FIXES`
