# Decision: Batch04H 2026门头沟一模 Next Step

Run: 选必一《当代国际政治与经济》四线终极全书 2026-05-03

Role: Codex A / Decision Maker

Scope: 只裁决 Batch04H 下一步；不改总表、不改学生稿、不宣布最终成品。

## Status

Codex A 已把 `2026门头沟一模 Q20` 写入 candidate prelim，`Q21` 保持 `boundary_only_expression_accumulation`。当前 `screen -ls` 仍显示 `xuanbiyi_claudecode_batch04H_20260503` detached，Cross-thread guard 继续生效。

## Decision

Batch04H 不能立刻做最终闭合。应等待 ClaudeCode B 明确退出，或由总控保存 B 线当前快照后，再由 Patcher/Governor 做 A/B 差异合闸。

允许现在做的事：
- Patcher 预读 Codex A 的 `fusion/scoring_atom_table_batch04H_mengtougou2026_prelim.csv` 与 `fusion/merge_register_batch04H_mengtougou2026_updates.md`。
- 预设冲突清单：`两种市场两种资源` 的题型方向边界、`开放型世界经济` 是否过度合并、`高水平开放新范例` 是否只作制度型开放表述积累、`Q21` 是否保持 boundary-only。
- Codex生产可准备下一批 source locator，但不得把 Batch04H 写成 closed。

必须等待的事：
- ClaudeCode B 退出或快照确认。
- A/B 差异文件被读取并裁定。
- Patcher 给出 `PASS_AFTER_AB_REVIEW` 或 `FIX_REQUIRED`。
- Governor 给出 `PASS_AFTER_AB_REVIEW` 或 `BLOCK`。

## Student Docs

学生稿、六桶索引、按题视图、Word/PDF、FINAL_ACCEPTANCE、coverage close 全部继续阻断。Batch04H 目前只能算 `prelim_atoms_written / review_pending`，不能进入学生正文。

## Next Batch After Closure

Batch04H 闭合后，下一批建议进入 `Batch04I 2026丰台一模 Q19 source-reverify`。

理由：
- 2026一模仍是高权重补遗源。
- 本地源清单已有 `2026丰台一模` P0 细则与 P3 试卷。
- 旧线索中已有 `2026丰台一模_Q19` 相关记录，但只能作后置比对，必须回原始细则/试卷重核。

后备顺序：`2026延庆一模 Q19(2)` -> `2026房山一模` -> `2026石景山一模`。`2026石景山期末` 仍全模块排除，不得误回补。
