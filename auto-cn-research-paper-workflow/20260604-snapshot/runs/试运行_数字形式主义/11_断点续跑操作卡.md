# 断点续跑操作卡

## 当前断点

- 时间：2026-06-04 02:13
- 页面：人大图书馆代理知网 `数字形式主义` 检索页及候选检索页
- 状态：用户已手动通过本轮拼图；Chrome 标签页可见 `libproxy.ruc.edu.cn/bar/verify/verifySuccess.html`。后续续跑证明：人大代理知网首页和已加载详情页可接管、可读标题 URL、可截图；旧 `数字形式主义` 检索页、DOM 控件读取和新题名检索仍会触发超时；Computer Use 因无法可靠确认当前浏览器 URL 而停止。
- human_in_the_loop_required：遇到拼图、验证码、滑块、SSO、身份确认、下载确认、权限提示或支付/机构权限确认时必须为 `true`；由用户本人完成后再续跑，Codex 不代过、不绕过。
- 处理：当前不是等待用户通过本轮拼图，而是浏览器准入验收未通过。先从可控的人大代理知网首页或已加载详情页恢复，而不是优先复用旧检索页；恢复后再从 C-001/C-002/C-003 续跑。

## 续跑前检查

1. 读取 Chrome 当前标签页标题和 URL。
2. 确认 URL 仍在 `libproxy.ruc.edu.cn` 代理域名下，或由人大图书馆电子资源平台跳转进入。
3. 确认浏览器控制工具能稳定读取当前页标题、URL 和正文文本，不只是能列出标签页。
4. 运行或更新 `12_浏览器准入验收.md`；只有 `browser_path_status=pass` 且 `current_url_status=pass`，才继续点击详情页或下载。
5. 确认页面不再显示 `拼图`、`验证码`、`滑块`、`安全验证`、`SSO`、`身份确认` 等字样。
6. 若仍显示验证页面，停止；不点击、不拖动、不拼接接口。

## 续跑顺序

按 `10_候选下载队列.md` 当前顺序执行。恢复入口优先级为：人大代理知网首页重新检索 > 已加载详情页回退/跳转 > 旧检索页；旧检索页已多次导致控制超时，不作为首选。

1. C-001：《为“注意力”而生产：乡村数字治理形式主义创造的一种新解释》
2. C-002：《乡村治理现代化中“数治负能”的消解逻辑——基于东部 Z 镇的经验研究》
3. C-003：《迈向“减负增效”：政务应用程序规范化管理的理论逻辑、实践路径与未来发展》

若其中某篇无法获得 PDF 或 HTML 正文，记录为候选并顺延到 C-004/C-005/C-006。

## 每篇文献操作顺序

1. 从人大代理检索页点击题名进入详情页。
2. 在详情页记录题名、作者、来源、时间、DOI/URLID、关键词、摘要和可见下载入口。
3. 优先点击页面可见的 `PDF下载`。
4. 如果只有 `CAJ下载` 或 `HTML阅读`，先尝试 `HTML阅读` 是否可读；不可读则登记为 `needs_pdf_or_reader`。
5. 不直接拼接或打开知网 `download/order` 接口。
6. 下载后运行：

```powershell
$env:PYTHONUTF8='1'
python C:\Users\Administrator\.codex\skills\auto-cn-research-paper\scripts\extract_pdfs.py --pdf-dir C:\Users\Administrator\Downloads --pattern "<标题关键词>*.pdf" --out-dir C:\Users\Administrator\Documents\论文写作\fulltext_extract --manifest C:\Users\Administrator\Documents\论文写作\试运行_数字形式主义\fulltext_manifest.md --skip-existing
```

7. 更新 `03_文献矩阵.md`、`04_优秀论文范式提取.md`、`06_论文初稿.md`、`07_引用与证据审查.md` 和 `09_完成度审计.md`。
8. 运行正式门槛审计：

```powershell
$env:PYTHONUTF8='1'
python C:\Users\Administrator\.codex\skills\auto-cn-research-paper\scripts\run_audit.py C:\Users\Administrator\Documents\论文写作\试运行_数字形式主义 --min-fulltext 8
```

## 停止条件

出现登录、拼图、验证码、滑块、SSO、身份确认、下载确认、权限警告、下载限制或 URL 无法确认时立即停止，并记录断点，同时将 `human_in_the_loop_required=true` 写入状态记录；用户本人完成后再继续。
