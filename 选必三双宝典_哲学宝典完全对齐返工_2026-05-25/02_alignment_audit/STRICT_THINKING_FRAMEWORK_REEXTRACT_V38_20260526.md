# V38 严格重读思维框架 PDF

verdict: `FRAMEWORK_MISMATCH_CONFIRMED_REWRITE_REQUIRED`

用户指出当前思维宝典框架没有严格遵循桌面 PDF：科学思维下自创了 `探索性与方法更新`、`整体安排`；辩证思维下合并了 `整体性与系统观念`、`动态性、质量互变与发展过程`；创新思维下自创了 `改变条件与建立新联系`。本轮重新从 PDF 渲染图与文本层抽取框架，确认用户判断成立。

## 框架 PDF

- `/Users/wanglifei/Desktop/先前框架/逻辑与思维 思维部分 原文件 拷贝.pdf`
- 页数：14 页
- 本轮渲染图：`02_alignment_audit/framework_pdf_extract_20260526/contact_1.png` 至 `contact_4.png`

## 目标正文框架

### 科学思维

只保留三点：

1. `追求认识的客观性`
2. `结果具有预见性`
3. `结果具有可检验性`

以下标题不得作为科学思维并列节点：

- `探索性与方法更新`
- `整体安排`

### 辩证思维

按 PDF 顺序重排，不能合并乱序：

1. `整体性`
2. `动态性`
3. `分析与综合`
4. `矛盾分析法`
5. `量变与质变`
6. `适度原则`
7. `辩证否定`
8. `认识发展历程`

其中 `分析与综合` 内部可讲定性分析、定量分析、功能分析、因果分析、结构综合、功能综合，但这些不宜随意升为并列总节点。

`认识发展历程` 应服务 `感性具体 -> 思维抽象 -> 思维具体`，思维抽象方法包括分离、提纯、简略化、理想化。

### 创新思维

按 PDF 顺序重排：

1. `特征与三新`
2. `联想思维`
3. `发散思维与聚合思维`
4. `逆向思维`
5. `超前思维`

`联想思维` 内部讲跨越的联结性、非逻辑制约的畅想性，以及迁移、想象两种方法。

`发散思维与聚合思维` 内部讲检核表法、信息交合法、头脑风暴法，以及发散与聚合是创新思维两翼。

`逆向思维` 内部讲结构顺序反向、存在状态转换、功能反向、因果关系交换。

`超前思维` 内部讲探索性、预测性、不确定性，以及矛盾分析法、推理和想象、调查研究等方法。

## 当前稿硬失败点

- `一、科学思维` 下有 `探索性与方法更新`、`整体安排`，与 PDF 不符。
- `二、辩证思维` 下有 `整体性与系统观念`，应改为 `整体性`。
- `二、辩证思维` 下有 `动态性、质量互变与发展过程`，应拆成 `动态性` 与 `量变与质变`，且 `量变与质变` 应在分析综合、矛盾分析法之后。
- `二、辩证思维` 下 `矛盾分析与适度原则` 应拆成 `矛盾分析法` 与 `适度原则`。
- `二、辩证思维` 下 `辩证否定与扬弃` 应改为 `辩证否定`。
- `三、认识发展历程` 目前单独作为一级标题，需按 PDF 纳入辩证思维框架或至少在目录层级上服从辩证思维。
- `四、创新思维` 下有 `思路新、方法新、结果新`，应改为 `特征与三新`。
- `四、创新思维` 下有 `改变条件与建立新联系`，不是 PDF 并列节点，必须撤销并将题目归入 `联想思维`、`特征与三新`、`超前思维` 等真实节点。
- `联想思维` 当前排序在后，应按 PDF 放在 `特征与三新` 之后、`发散思维与聚合思维` 之前。

## 执行裁决

- V37 Word/Markdown 只能作为待返工底稿，不再建议用户继续按它审核框架。
- 下一步先重排思维宝典目录和 H2 节点，再处理同题多角度漏挂。
- 重新生成 Word 前必须跑 H2 标题白名单检查；白名单外标题出现即阻断交付。
